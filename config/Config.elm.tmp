module Config exposing (config, logoutUrl)

import Iknaio
import Theme.Theme exposing (Theme)


logoutUrl : String
logoutUrl =
    "{{VITE_LOGOUT_URL}}"


config : { theme : Theme }
config =
    { theme = Iknaio.theme
    }
